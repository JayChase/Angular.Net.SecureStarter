'use strict';

describe('app.security restoreUserService', function () {
   
    beforeEach(module('app.security'));

    beforeEach(function () {       
           
    });   
    
    //TODO this all needs changing for new functionality
    it('if path does not match any routes does not re-route', inject(function () {
        
       
        }));
    
    //Teardown
    afterEach(function () {
       
    });
});