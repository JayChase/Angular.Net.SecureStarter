(function () {
    'use strict';

    var controllerId = 'shellCtrl';

    angular.module('app.shell').controller(controllerId,
        ['$scope', shell]);

    function shell($scope) {
           
    }
})();
